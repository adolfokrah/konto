# Test Environment Configuration
NODE_OPTIONS="--no-deprecation --no-experimental-strip-types"
NODE_ENV=test

# Database Configuration for Testing
DATABASE_URI_TEST=mongodb://localhost:27017/payload-test

# PayloadCMS Configuration
PAYLOAD_SECRET=test-secret-key-for-local-development
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Email Configuration (for tests, console logging is fine)
# No email adapter configuration needed - tests will use console output
